{
  "name": "book-inventory-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "description": "Backend (Node.js + Express + Mongoose) ------------------------------------- 1. Copy .env.example to .env and replace the MONGO_URI placeholder with your Atlas connection string. 2. Install dependencies:    npm install 3. Start server in development:    npm run dev 4. API endpoints:    POST /api/books    GET  /api/books    PUT  /api/books/:id    DELETE /api/books/:id",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
